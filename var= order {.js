var= order {
    name= "Angie", Ben, Chad
    sugar= 1, 2, 3
    ready= true, false, true
}
console.log(order.name + order.sugar);
for (var i = 0; 1 === order.sugar(i); i++);
    console.log (order.name + "has ordered" + order.sugar);

for var j = 0; order.ready
